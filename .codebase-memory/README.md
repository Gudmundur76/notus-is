# Codebase Memory Graph

Knowledge graph of `Gudmundur76/notus-is` generated by [codebase-memory-mcp](https://deusdata.github.io/codebase-memory-mcp/) v0.8.1.

| Metric | Value |
|---|---|
| Nodes | 2223 |
| Edges | 5140 |
| Compressed size | 10MB |
| Last indexed | 2026-06-26 |

## Bootstrap in a new session

```bash
# Install binary (one-time)
curl -fsSL https://raw.githubusercontent.com/DeusData/codebase-memory-mcp/main/install.sh \
  -o /tmp/cbm-install.sh && bash /tmp/cbm-install.sh --skip-config
export PATH="$HOME/.local/bin:$PATH"

# Restore graph
mkdir -p ~/.cache/codebase-memory-mcp
gunzip -c .codebase-memory/graph.db.gz > ~/.cache/codebase-memory-mcp/Gudmundur76-notus-is.db

# Verify
codebase-memory-mcp cli index_status '{"project":"Gudmundur76-notus-is"}'
```

## Auto-update

`.github/workflows/codebase-memory.yml` re-indexes on every push to `main`.
